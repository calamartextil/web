'use client';

import { useCartContext } from '@/app/contexts/CartContext';

interface TelaGraphProps {
  sku: string;
}

export default function TelaGraph({ sku }: TelaGraphProps) {
  return <div>TelaGraph</div>;
}
